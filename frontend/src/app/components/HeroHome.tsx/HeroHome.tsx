import React from 'react';
import './style.scss';

const HeroHome = () => {
    return (
        <section className='hero_home'>
            <h2>site builder</h2>
        </section>
    );
};

export default HeroHome;
